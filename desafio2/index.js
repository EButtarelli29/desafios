let nombre = "tornillos";
let precioU = 15;
let cantidad = prompt("Ingrese la cantidad de " + nombre + " deseada: ");

alert(`${cantidad} ${nombre} costarán $${precioU*cantidad}.`);

//Swal.fire("SweetAlert2 is working!");